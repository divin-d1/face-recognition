.venv/
__pycache__/
*.pyc

# ArcFace ONNX model (downloaded during setup, not committed)
models/*.onnx
buffalo_l.zip
*.onnx

# Personal biometric data: aligned enrollment crops and the enrollment DB
# (embeddings + face images of real people must never be committed)
data/enroll/*
!data/enroll/.gitkeep
data/db/*
!data/db/.gitkeep

# Debug artifacts
data/debug_aligned/
